/**
 * Layer scale. Use these class names instead of ad-hoc z-index values.
 * 30: fixed page navigation
 * 40: iteration switcher and skip link
 */
export const layer = {
  nav: "z-30",
  switcher: "z-40",
} as const;
